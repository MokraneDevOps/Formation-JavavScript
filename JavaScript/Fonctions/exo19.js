// EXERCICE 19

// Début de votre code
const auCarre = (number) => {
    return number * number; // Renvoie le nombre au carré
  };
  // Fin de votre code
  
  // Exemples d'utilisation
  console.log(auCarre(4));  // Va afficher 16 dans le terminal
  console.log(auCarre(2));  // Va afficher 4 dans le terminal
  console.log(auCarre(-3)); // Va afficher 9 dans le terminal
  