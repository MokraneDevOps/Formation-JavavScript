const calculateAge = (b, a) => {
    // Notez que la fonction s'attend à recevoir l'année de naissance en premier argument (b) et l'année en cours en deuxième argument (a)
    console.log(a - b); // Doit afficher `34` dans le terminal
  };
  
  const yearOfBirth = 1987;
  const currentYear = 2021;
  
  // Début de votre code
  calculateAge(yearOfBirth, currentYear); // Appel de la fonction avec l'année de naissance en premier et l'année actuelle en second
  // Fin de votre code
  