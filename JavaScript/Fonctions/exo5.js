const calculateAge = (a, b) => {
    // Notez que la fonction s'attend à recevoir l'année en cours en premier argument (a) et l'année de naissance en deuxième argument (b)
    console.log(a - b); // Doit afficher `34` dans le terminal
  };
  
  const yearOfBirth = 1987;
  const currentYear = 2021;
  
  // Début de votre code
  
  calculateAge(currentYear, yearOfBirth); // Appel de la fonction avec les paramètres dans le bon ordre
  
  // Fin de votre code
  