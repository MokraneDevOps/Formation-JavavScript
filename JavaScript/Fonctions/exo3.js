const displayFullName = (firstName, lastName) => {
    console.log(firstName + " " + lastName); // Doit afficher `Victor Hugo` dans le terminal
  };
  
  // Début de votre code
  
  displayFullName("Victor", "Hugo"); // Appel de la fonction avec les arguments "Victor" et "Hugo"
  
  // Fin de votre code
  