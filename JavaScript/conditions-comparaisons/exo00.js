// Sachant que :
const V = true;
const F = false;

// Voici quelques opérations logiques :
console.log(V && V);   // true
console.log(V && F);   // false
console.log(V || F);   // true
console.log(V || !F);  // true
console.log(V && !V);  // false
console.log(V & V & V & V & F);  
