// EXERCICE 1
const arr = ['pomme', 'poire'];

// début de votre code
arr.pop(); // Retire le dernier élément du tableau
// fin de votre code

console.log(arr); // doit afficher ['pomme']


// explication :
// arr.pop() : La méthode pop retire le dernier élément du tableau arr. Ici, elle va retirer l'élément "poire".
// Ensuite, tu utilises console.log(arr) pour vérifier que le tableau ne contient plus que "pomme".
